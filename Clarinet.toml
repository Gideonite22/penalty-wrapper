[project]
name = "penalty-wrapper"
authors = ["Your Name"]
description = "A smart contract for managing asset penalties and compliance mechanisms"
telemetry = false
requirements = []
[contracts.penalty-wrapper]
path = "contracts/penalty-wrapper.clar"
depends_on = []

[repl]
costs_version = 2
parser_version = 2

[repl.analysis]
passes = ["check_checker"]

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false
